{
  "name": "sitetrack",
  "version": "0.1.0",
  "private": true,
  "description": "ระบบติดตามงานผู้รับเหมา — GS + GAS + Cloudflare Pages + LINE",
  "scripts": {
    "dev": "node frontend/server.js",
    "deploy:web": "wrangler pages deploy frontend --project-name sitetrack",
    "push:gas": "cd backend && clasp push",
    "open:gas": "cd backend && clasp open"
  },
  "keywords": ["construction", "contractor", "line", "google-apps-script", "cloudflare-pages"],
  "license": "UNLICENSED",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "wrangler": "^3.0.0"
  }
}
